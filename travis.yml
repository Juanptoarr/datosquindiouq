language: node_js
node_js:
  - '16'
cache:
  directories:
    - node_modules
install:
  - npm install
script:
  - npm run build
deploy:
  provider: script
  skip_cleanup: true
  script: npx railway deploy --env production
  provider: railway
  token: 
  on:
    branch: Victor
